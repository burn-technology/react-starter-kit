//IMPORT NECESSARY FILE

const {{camelCase name}} = (state = ..., action) => {
  switch (action.type) {
    case :
      return newState;
  
    default:
      return state;
  }
}

export default {{camelCase name}};